# OC-009 — packages/opencode/src/provider/provider.ts

- source_id/item_id：`OC-009`
- source_path：`packages/opencode/src/provider/provider.ts`
- source_hash：`77216c71fd0079da036ac25af516a29ee45f58b52e7c63987065ca97583ee364`
- source_bytes：`80947`
- source_lines：`2072`
- coverage：已按源文件顺序读取完整字节范围 `0-80946`（80947 字节）与完整行范围 `L1-L2072`；本笔记中的引用均使用 `Lx-Ly`。

## 完整行为复盘

**依赖、常量、类型与动态 provider 表（L1-L151）。** 文件把 `effect`、`ModelsDev`、`Auth`、`Env`、`Plugin`、`FSUtil`、`RuntimeFlags` 与 AI SDK 接在一个 provider service 上。`OPENAI_HEADER_TIMEOUT_DEFAULT` 是 300000ms（L35）。`BundledSDK` 要求 `languageModel`，可选 `chat`/`responses`（L107-L111）；`BUNDLED_PROVIDERS` 将一组 npm 名称映射为懒加载 `create*` 工厂，避免启动时加载全部 SDK（L113-L140）。`CustomModelLoader`/`CustomVarsLoader`/`CustomDiscoverModels`/`CustomLoader` 描述自定义装载、变量替换、发现模型和 `autoload` 决策（L142-L151）；`CustomDep` 以 Effect 形式提供 auth/config/env/get（L153-L158）。

**流控辅助函数。** `wrapSSE(res, ms, ctl)` 仅在 `ms` 为正数、存在 body 且 content-type 含 `text/event-stream` 时包装；每次 `reader.read()` 用独立计时器，超时构造 `ProviderError.ResponseStreamError("SSE read timed out")`，同时 `AbortController.abort` 与 `reader.cancel`，拒绝当前 pull；正常结束关闭流，cancel 会向上游 abort/cancel（L37-L83）。这保证“头已到但 SSE 长时间无 chunk”可被检测，且 backpressure 由 `ReadableStream.pull` 保留。`timeoutController(ms)` 建立头部超时 controller，定时 abort `HeaderTimeoutError(ms)`，返回 signal 与 `clear`（L85-L92）。`googleVertexAnthropicBaseURL(project, location)` 只接受 project 且 location 为 `eu`/`us`，生成 regional `rep.googleapis.com` URL，否则 `undefined`；`googleVertexEndpoint` 将 `global`、`eu`、`us` 和其他 location 映射到 Vertex 域名（L94-L105）。`selectAzureLanguageModel` 按 `useChat`、`responses`、`messages`、`chat`、`languageModel` 顺序选 API（L160-L166）；`selectBedrockMantleLanguageModel` 对两个 safeguard ID 优先 `chat`，其他优先 `responses`（L168-L172）。

**`custom(dep)` 与内置 provider 规则（L174-L1005）。** 返回以 provider ID 为键的 `CustomLoader`：

- `anthropic` 关闭 autoload，仅注入 interleaved/fine-grained tool streaming beta header（L176-L184）。
- `opencode` 先查 env、auth、config apiKey；无凭据时删除所有非零 input cost 模型，仅保留免费模型，并以 `apiKey: "public"` 运行；`autoload` 由剩余模型数决定（L185-L207）。
- `openai`、`meta`、`xai` 强制 `sdk.responses(modelID)`，其中 openai 默认 `headerTimeout=300000`（L208-L230）。`github-copilot` 无 responses/chat 时退回 `languageModel`；有模型 endpoint 元数据时遵从它，否则 `gpt-数字` 且数字>=5（排除 `gpt-5-mini`）走 responses，其余 chat（L231-L245）。
- `azure` 从 provider option、API auth metadata、OAuth accountId、`AZURE_RESOURCE_NAME` 取资源名；无 resource 且无 baseURL 时不 autoload，getModel 抛明确错误；有值时按 `useCompletionUrls` 选 API，并通过 `vars` 注入资源名（L246-L285）。`azure-cognitive-services` 读取资源名，生成 cognitiveservices baseURL（可切 deployment URL），同样选择 API（L287-L300）。
- `amazon-bedrock` 的 region 优先级为 config→`AWS_REGION`→`us-east-1`，profile 为 config→`AWS_PROFILE`；bearer token 优先于 credential chain，必要时异步加载 `fromNodeProviderChain`，endpoint 优先于 baseURL（L301-L375）。`getModel` 保留 ARN、`global./us./eu./jp./apac./au.` 前缀；否则按 us/eu/ap 区域和模型族添加跨区前缀，并跳过 GovCloud，最终 `sdk.languageModel`（L374-L464）。
- `llmgateway`/`openrouter`/`zenmux`/`kilo`/`vercel`/`nvidia`/`cerebras` 关闭 autoload 并设置归因 headers；nvidia 仅 config 来源自动加载（L467-L508、L604-L613、L885-L903）。
- `google-vertex` 从 option、四种 GCP env 取 project，location 默认 `us-central1`；无 project 不 autoload；有 project 时 vars 提供 project/location/endpoint，fetch 用 `GoogleAuth` 取 bearer token，model ID trim 后传给 SDK（L509-L559）。`google-vertex-anthropic` 仅从 GCP env 取 project，location 默认 global，必要时使用 regional Anthropic baseURL（L560-L579）。`sap-ai-core` 从 process.env 或 API auth 写入 `AICORE_SERVICE_KEY`，读取 deployment/resource group，只有 service key 存在才 autoload，model 通过 `sdk(modelID)`（L580-L603）。
- `gitlab` 动态加载 workflow discovery；token 优先 OAuth access/API key，再 env；设置 User-Agent、beta header、feature flags；`duo-workflow-*` 选择静态 workflow 或 `duo-workflow` 并可附 `selectedModelRef`，其他走 `agenticChat`。发现模型失败或空结果返回空表；新模型以完整 `Model` 元数据加入（L614-L738）。
- `cloudflare-workers-ai` 在已有 baseURL 时跳过 account 校验；否则 account 来自 env/auth metadata，缺失时 getModel 抛错；有 token 才 autoload，并注入 User-Agent 与 account 变量（L739-L776）。`cloudflare-ai-gateway` 要求 account、gateway、token，动态装载 gateway/openai/anthropic/unified/compat SDK；openai 前缀走 native Responses，anthropic 将点替换成短横线后走 Messages，Workers AI（`workers-ai/` 或 `@cf/`）走 unified，其余第三方走 Cloudflare REST OpenAI-compatible 并带 gateway ID（L777-L884）。其中 metadata 可由显式 option 或 `cf-aig-metadata` JSON 解析，解析失败静默为 undefined（L819-L843）。
- `snowflake-cortex` 按 env→API auth→OAuth→config 取 account/token；缺失时返回不 autoload 且 getModel 抛错；非纯 OAuth token 时包裹 fetch，把 `max_tokens` 改成 `max_completion_tokens`，将“conversation complete”400 转成空成功响应，并在 SSE 中把空 role 改为 assistant；最终仅 config 来源 autoload（L904-L1003）。

**Schema、导出数据结构与错误（L1007-L1219）。** `ProviderApiInfo`、`ProviderModalities`、`ProviderInterleaved`、`ProviderCapabilities`、`ProviderCacheCost`、`ProviderCostTier`、`ProviderCost`、`ProviderLimit` 定义模型 API、模态、推理字段、能力、价格和上下文边界（L1007-L1076）。导出的 `Model` 包含 `id/providerID/api/name/family/capabilities/cost/limit/status/options/headers/release_date/variants`，`Info` 包含 provider 身份、来源（env/config/custom/api）、env/key/options/models；`ListResult` 与 `ConfigProvidersResult` 分别表示完整列表和配置列表（L1078-L1119）。`toPublicInfo` 深拷贝 JSON，过滤不符合 `Model` schema 的模型，并丢弃 function/symbol/undefined、把 bigint 转字符串（L1121-L1135）。`defaultModelIDs` 对每个 provider 排序后取首模型 ID，空 models 会在 `[0].id` 处失败（L1137-L1139）。`ModelNotFoundError` 记录 provider/model/suggestions/cause，消息带最多建议；`InitError` 包 providerID/cause；`NoProvidersError` 与 `NoModelsError` 给出稳定消息；每个类都有 `isInstance`（L1141-L1193）。`Interface` 暴露 `list/getProvider/getModel/getLanguage/closest/getSmallModel/defaultModel` Effect 签名（L1195-L1206）；`State` 保存语言模型缓存、活跃 providers、catalog、SDK、model/vars loaders（L1208-L1215）；`Service` 与 `use` 是 Effect service 导出（L1217-L1219）。

**目录归一化与建议。** `cost` 将 `ModelsDev` 价格缺失值归零，复制 tiers 和 `context_over_200k`（L1221-L1252）。`cloudflareGatewayNpm` 为 gateway 的 openai/anthropic 模型选择 native npm，否则 undefined（L1254-L1263）。`fromModelsDevModel` 将 catalog 模型转为 `Model`，默认 npm 为 openai-compatible，能力和模态缺失时取 false（toolcall 默认 true），再由 `ProviderTransform.reasoningVariants` 或 `variants` 计算变体（L1265-L1320）。`fromModelsDevProvider` 遍历模型并为 experimental modes 生成 `${model.id}-${mode}`，合并价格、body 转换出的 options 和 headers，来源固定 custom（L1322-L1347）。`modeOptions` 把 snake_case body key 转 camelCase；OpenAI reasoning object 的 mode 改名为 `reasoningMode`（L1349-L1358）。`modelSuggestions` 过滤 deprecated、按运行时开关过滤 alpha，先用 fuzzysort（最多 3 个、threshold -10000），无结果再按 query token 命中数和 ID 排序取 3 个（L1360-L1387）。

**Layer 初始化与 provider 合并（L1389-L1730）。** `layer` 获取依赖，建立 `InstanceState<State>`；初始读取 config/models-dev，构造 `catalog` 与公开 database，随后载入插件（保证 plugin config hook 先于 cfg.provider），读取 enabled/disabled 集（L1389-L1453）。内部 `mergeProvider` 只把已有数据库 provider 合并进 providers，未知 provider 忽略；`isProviderAllowed` 执行 allowlist 优先、denylist 次之（L1427-L1438、L1448-L1452）。插件 models hook 可替换完整 models 并规范化 ID/providerID；配置 provider 合并 name/env/options/models，模型逐项继承 catalog 并设置能力、cost、limits、headers、variants；OpenAI-compatible 的 DeepSeek 新模型默认 interleaved reasoning_content（L1454-L1579）。随后依次载入 env key、API auth、插件 auth loader、自定义 loader；loader 的 `autoload` 或已有 provider 才会写入 options/loaders（L1582-L1645）。GitLab discovery 仅在允许且 provider 存在时运行，异常吞掉（L1657-L1669）。最后删除不允许、alpha（开关关闭）、deprecated、黑名单/不在白名单及内置 chat alias 的模型，生成默认 variants；无模型 provider 删除（L1671-L1719）。状态初始化是一次 Effect，后续调用经 `InstanceState` 读取，SDK/model maps 作为进程内缓存。

**SDK 解析和接口实现。** `resolveSDK(model,s,envs)` 复制 provider options；为 Vertex Anthropic 补 baseURL，非 openai-compatible 的 Vertex 删除 fetch，openai-compatible 默认 `includeUsage=true`；先由 vars loader 替换 `${KEY}`，再用 env 替换未解析变量；注入 provider key、model headers；按 provider/npm/options 的 hash 复用 SDK（L1734-L1797）。它从 options 取 `chunkTimeout` 默认 300000、`headerTimeout` 默认 300000，删除这两个字段后包裹 fetch：合并调用方 signal、chunk controller、header controller 和 `AbortSignal.timeout`，关闭 Bun 内置 timeout，响应返回 SSE wrapper；SDK 先查 bundled factory，否则 `Npm.add` 安装包、导入 entrypoint、选首个 `create*` 导出；任一异常统一包装 `InitError`（L1798-L1865）。`getProvider` 直接按 ID 返回，缺失值是 undefined（L1868-L1870）。`getModel` 先检查活跃 provider，再查 catalog/provider 生成 suggestions，找不到返回 `ModelNotFoundError`（L1872-L1894）。`getLanguage` 以 provider/model 为 key 做并发不加锁的缓存；resolve SDK 后调用自定义 model loader 或 `sdk.languageModel`，把 `NoSuchModelError` 转成 `ModelNotFoundError`（L1896-L1925）。`closest` 按 query 顺序和 provider model insertion order 找第一个包含项（L1927-L1937）。`getSmallModel` 优先 config.small_model，找不到吞掉错误；其次给插件 experimental hook；Azure 明确返回 undefined；按 provider 特殊族优先级、release_date/id 降序选模型，Bedrock 再优先 global、区域前缀、无前缀（L1939-L2006）。`defaultModel` 优先 config.model；再读 `${Global.Path.state}/model.json` 的 recent 数组，坏文件/IO 视为空；选择首个仍存在的 provider/model，否则按 config provider allowlist 找 provider，排序取首模型，分别抛 `NoProvidersError`/`NoModelsError`（L2008-L2045）。

**排序、解析与节点导出（L2047-L2072）。** `sort` 以 `priority=[gpt-5,claude-sonnet-4,big-pickle,gemini-3-pro]` 的命中优先级降序、`latest` 优先、ID 降序排序；`smallModelFamilyPriority` 为 gemini-flash/gpt-nano/claude-haiku（L2047-L2056）。`parseModel` 只在第一个 `/` 切 provider，余下部分重新 join，支持模型 ID 内含 slash（L2058-L2064）。`node` 将 Service layer 与 FS/Config/Auth/Env/Plugin/ModelsDev/RuntimeFlags 依赖组合；文件末尾 `export * as Provider from "./provider"` 形成命名空间导出（L2066-L2072）。

## 状态、取消、恢复与副作用

- 初始化状态由 `InstanceState` 持有；`State.models` 与 `State.sdk` 是进程内缓存，SDK key 包 provider、npm、完整 options hash，因此相同配置复用，不同 key 并行构造（L1208-L1215、L1788-L1797）。`getLanguage` 先查缓存但没有 per-key promise 锁，两个并发首访可能各自初始化，最后写入同一个 key（L1896-L1918）。
- 取消主要由请求侧 `AbortSignal`、header timer、SSE chunk timer 和 `AbortSignal.timeout` 联合；header timer 在 fetch finally 清理，chunk timer 在每次 read 独立清理；SSE cancel 会 abort 上游（L1804-L1828）。源没有 provider 层自动重试；超时由 `ProviderError` 交给上层 retry 策略。SDK npm 安装/动态 import 是外部副作用，失败统一 `InitError`，没有回滚已安装包（L1842-L1865）。
- 认证与环境存在显式副作用：Bedrock 和 SAP loader 直接写 `process.env`（注释说明 `Env.set` 只是浅复制），Google Vertex fetch 每次请求获取 ADC token，GitLab discovery 发外部网络请求，Cloudflare/Snowflake 自定义 fetch 改写请求/响应（L319-L329、L582-L592、L542-L551、L681-L735、L945-L997）。
- 持久化只出现在 `defaultModel`：读取 `Global.Path.state/model.json`，不写回；recent 数据不合法就丢弃，配置模型和当前 provider/model 必须实际存在（L2008-L2041）。模型 catalog、provider merge、plugin discovery 均在实例初始化内存完成，没有源内事务或跨进程锁。
- loader 错误策略不一致：核心 SDK 初始化抛 `InitError`；GitLab discovery 异常吞掉为空；缺失凭据的多数 provider 返回 `autoload:false` 并把错误延迟到 getModel；Cloudflare gateway 缺 token 则在 loader 直接 throw（L739-L811）。

## 源内测试与行为判据

同目录存在直接测试。`packages/opencode/test/provider/provider.test.ts` 验证 env/config 来源合并、disabled/enabled provider、模型 whitelist/blacklist、custom alias、alpha 开关、DeepSeek interleaved 默认、配置 options 合并、`getModel/getLanguage`、无效 provider/model 错误、`parseModel` 的 slash 语义及 `defaultModel` 的配置/无 provider 行为（L112-L420）。可独立判据：给定 env/config 后 `Provider.use.list()` 的 provider source、options 和模型集合必须与上述断言一致；非法查找必须是 Failure；`parseModel("openrouter/anthropic/claude-3-opus")` 的 modelID 必须保留内部 slash（L338-L348）。`header-timeout.test.ts` 验证 header 到达后不受 headerTimeout 影响、默认 300000、chunkTimeout SSE 停顿抛 `ResponseStreamError("SSE read timed out")`、`false` 禁用、header 未到时抛响应头超时（L27-L209），并验证 OpenAI 默认/配置覆盖 headerTimeout（L211-L245）。`model-status.test.ts` 验证 catalog status 与 normalized `ModelStatus` 分离、Provider.Model 接受 active（L8-L60）。`amazon-bedrock.test.ts` 验证 region/profile 优先级、bearer token 自动加载、Mantle Responses/Chat 选择、endpoint、web identity 和 ARN/跨区 ID 不被错误改写（L70-L106、L108-L176、L178-L254）。`gitlab-duo.test.ts` 当前测试主体被注释，仅有 `export {}`，因此 GitLab 行为应以 discovery 代码和可独立的 fake SDK 验证为准（L1-L38）。

## zenpi Rust 映射

- **provider catalog/模型元数据**：`src/providers/registry.rs` 的 `ModelDescriptor`、`effective_capabilities`、`validate_reasoning`、`context_budget`、`digest` 已对应 `Model` 的能力、限制、推理与价格核心语义（L58-L119）。建议新增/扩展 `ProviderCatalog` 保存 `Info` 等价字段（来源、env、options、variants），并实现 `from_models_dev_model`、`cost`、`model_suggestions`；当前 registry 是静态、有界 catalog，源是动态 models.dev+config/plugin，差异是必须显式处理刷新、status alpha/deprecated 和未知 provider。
- **路由与 SDK 选择**：`src/providers/mod.rs` 的 `Protocol`、`RouteRule`、`ProviderDefinition`、`get_provider_definition` 与各 provider 文件把 openai/anthropic/google/deepseek/codex 的端点、header、能力、option policy 固化（L13-L20、L103-L182、L152-L224）；`src/providers/connection.rs::resolve_connection/resolve_model_route` 验证 model/provider、协议、URL、auth 和 streaming（L142-L240）。这对应源 `BUNDLED_PROVIDERS`、`custom` 与 `resolveSDK` 的“provider+model→实际 API”。建议落点为 `providers::registry::ProviderCatalog::resolve_model`、`providers::connection::prepare_route` 与 `backend::Backend::complete/stream`，保留 `ValidatedRoute` 不可被 encoder 替换。差异：源允许动态安装任意 npm `create*`，Rust 当前仅静态定义；源支持大量第三方 loader（Bedrock、Vertex、Cloudflare、Snowflake、GitLab），Rust 需明确新增 `ProviderDefinition` 或拒绝未知协议，不能用名称启发式代替。
- **请求/流式超时与取消**：`src/runtime.rs::CancellationToken` 是协作取消，要求 job 在 chunk/retry 边界检查，shutdown 有 grace（L45-L115、L300-L430）；`src/headless.rs` 有有界 provider/agent mailbox，慢 stdout 时丢弃并报告（L39-L51、L941-L1041）。建议在 `backend` fetch adapter 中加入 header/chunk deadline，映射 `ProviderError.HeaderTimeoutError`/`ResponseStreamError` 到 `BackendError::Transport`，每个 SSE chunk 调 `CancellationToken::is_cancelled`。差异：源把多种 `AbortSignal` 合并且允许 `false` 禁用，Rust 需定义 `Option<Duration>`/`None` 的等价配置并保证关闭时 join，不可 detach 未知 provider 工作。
- **会话、默认模型与恢复**：`src/session.rs` 是 append-only JSONL、interrupted operation 需显式 retry/abandon（L1422-L1585），`src/core.rs::Agent::new` 会读取恢复状态并发出“requires explicit retry”，模型选择通过 `set_model` 验证并写 `model_selected` 事件（L598-L640、L1167-L1188）；这比源仅读取 `model.json` recent 更强。建议实现 `ProviderSelectionStore` 写入 session event，`default_model` 先配置、再 recent、再排序；恢复时校验 provider/model digest，避免源的无版本 recent 被静默接受。`src/headless.rs` 的 reconnect journal、request replay 和 bounded terminal cache（L155-L256、L405-L497）可承接 provider 初始化/流式事件的重放，但源本身没有同等级幂等协议。
- **工具与审批副作用**：源 provider 只把 SDK/tool call 暴露给上层；zenpi `src/tool_runtime.rs` 要求 prepare 后再执行、取消前停止新调用、worker join 且未知副作用进入恢复（L130-L341），`src/approval.rs` 以 `ApprovalCoordinator` 等待 host 决策、支持取消和持久化接受记录（L122-L241、L367-L405）。建议 `ProviderRequestScope` 在 `src/core.rs` 中绑定 route digest、identity scope、policy/lease（L521-L555），任何 provider 请求都复用；差异是 zenpi 的审批/worker gate 比源更严格，应保持而不是把 provider `autoload` 当授权。
- **协议与导出接口**：`src/protocol.rs` 将 prompt/steer/cancel/status/resume/approve/shutdown 转成有界 `Command`（L211-L263、L265-L320、L415-L515），`src/headless.rs` 负责 JSONL 响应、资源和恢复。建议增加 `provider.list/get_model/get_language/closest/get_small_model/default_model` 的 host 命令或内部 API，并把 `ModelNotFound` suggestions 序列化成稳定错误；当前协议只承载 backend/provider 名称，不承载源的完整 `Info/Model` schema。

可执行验证清单：为 Rust 新增单元测试覆盖 `parse_model` slash、catalog merge 的 allow/deny/status、`model_suggestions` 最多三项、OpenAI/Anthropic/Google route header、SSE header/chunk timeout、取消时无 detached worker、SDK key 相同配置命中缓存、unknown model 返回 suggestions；运行现有 `cargo test` 与 headless JSONL 集成测试，比较上述源测试判据。

## 未决问题

1. `ModelsDev.Service.get()` 的 catalog 快照是否会在同一进程热刷新，源文件只展示一次 layer 初始化，无法确认刷新策略（L1403-L1406）。
2. `getLanguage` 首次并发 miss 是否由 Effect runtime 在实例级串行化，代码本身没有显式锁，无法确认是否可能重复 SDK 构造（L1896-L1918）。
3. `BUNDLED_PROVIDERS` 动态 import 工厂返回值及第三方 `create*` 导出唯一性由 npm 包保证，源未验证多导出或工厂返回 Promise 的边界（L113-L140、L1842-L1862）。
4. `defaultModelIDs` 在空模型 provider 上的异常是否由调用方先过滤，函数本身没有空数组防护（L1137-L1139）。
